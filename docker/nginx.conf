# ##/* server {
#     listen 80;

#     location /health {
#         return 200 'OK';
#         add_header Content-Type text/plain;
#     }

#     location / {
#         root /usr/share/nginx/html;
#         index index.html;
#         try_files $uri /index.html;
#     }
# }
# */


server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location /health {
        return 200 "OK";
        add_header Content-Type text/plain;
    }
}